# Dev Cleaner GitHub Pages

Static landing page for `dev-cleaner`, intended to be served from the `gh-pages`
branch of `github.com/sonald/dev-clean`.

The visual prototype used for implementation is saved at
`assets/landing-prototype.png`.
