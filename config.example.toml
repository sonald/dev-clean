# Dev Cleaner Configuration Example
# Copy this file to ~/.config/dev-cleaner/config.toml (Linux/macOS)
# or %APPDATA%\dev-cleaner\config.toml (Windows)

# Directories to always exclude from scanning
exclude_dirs = [
    ".git",
    ".svn",
    ".hg",
]

# Default maximum scan depth (None = unlimited)
# default_depth = 10

# Default minimum size in MB (None = no filter)
# min_size_mb = 100

# Default maximum age in days (None = no filter)
# max_age_days = 30

# Custom cleanable patterns
# Add your own project types and cleanable directories
[[custom_patterns]]
name = "Unity"
directory = "Library"
marker_files = ["Assets", "ProjectSettings"]
# marker_mode = "all_of" # optional (default: "any_of")

[[custom_patterns]]
name = "Unreal Engine"
directory = "Intermediate"
marker_files = [".uproject"]
# marker_mode = "any_of" # optional (default: "any_of")

# [[custom_patterns]]
# name = "Your Custom Project"
# directory = "custom-build-dir"
# marker_files = ["marker-file.txt", "another-marker.json"]

# ------------------------------------------------------------------------------
# Profile presets
# ------------------------------------------------------------------------------

# [scan_profiles.work]
# paths = ["~/Projects", "~/GitHub"]
# depth = 4
# min_size_mb = 50
# max_age_days = 7
# gitignore = false
# category = "build"        # cache/build/deps
# max_risk = "medium"       # low/medium/high

# ------------------------------------------------------------------------------
# Keep / Protect
# ------------------------------------------------------------------------------

# Exact paths to protect
# keep_paths = ["/Users/me/Projects/critical-app/node_modules"]

# Glob paths to protect
# keep_globs = ["~/Projects/critical-*/**/target"]

# Protect entire project roots
# keep_project_roots = ["~/Projects/never-clean-*"]

# ------------------------------------------------------------------------------
# Audit logging
# ------------------------------------------------------------------------------

# [audit]
# enabled = true
# path = "/Users/me/.local/share/dev-cleaner/operations.jsonl"
# max_size_mb = 5
